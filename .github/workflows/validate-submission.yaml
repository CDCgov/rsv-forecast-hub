name: "Hub Submission Validation (R)"

on:
  workflow_dispatch:
  pull_request:
    branches: main

permissions:
  contents: read
  pull-requests: read

jobs:
  validate-submission:
    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v7

      - name: "Get model output directory changes"
        id: "model_output_diffs"
        uses: step-security/changed-files@v47
        with:
          path: model-output
          dir_names: "true"

      - uses: hubverse-org/hubverse-actions/validate-submission@cbccdba4164bfb6fc5ab1a852cde4d4273181b31
        with:
          skip_submit_window_check: ${{ steps.model_output_diffs.outputs.all_modified_files == 'RSVHub-ensemble' }}
